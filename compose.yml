services:
  notebook:
    image: gelos:latest
    build: .
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
    volumes:
       - .:/app/ 
       - ${GELOS_RAW_DIR}:${GELOS_RAW_DIR}
       - ${GELOS_PROCESSED_DIR}:${GELOS_PROCESSED_DIR}
       - ${GELOS_EXTERNAL_DIR}:${GELOS_EXTERNAL_DIR}
    command: jupyter lab --ip=0.0.0.0 --allow-root
    ports:
       - "${HOST_PORT}:8888"
    shm_size: '32gb'
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  test:
    image: gelos-test:latest
    build: 
      context: .
      target: test
    volumes:
      - .:/app/
    command: make test